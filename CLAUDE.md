# 시작 전 필독

이 프로젝트를 열면 **다른 무엇보다 먼저 `작업메모장.md`를 처음부터
끝까지(맨 아래 최신 항목까지) 전부** 읽을 것. `src/`나
`template.html`/`dist/taleforge.html`을 스캔하기 전에, 이 메모장으로
"지금까지 뭘 했고, 왜 했고, 뭐가 남았는지"부터 파악한다. 세션이
끊기거나(새 창, 새 대화) 컨텍스트가 요약되어도 이 파일 하나만 읽으면
바로 이어서 작업할 수 있어야 한다는 게 이 파일의 존재 이유다.

⚠️ **이 파일은 2000줄이 넘는다 — 앞부분만 읽고 멈추지 말 것.**
파일 맨 위는 오래된 과거 기록이고, **진짜 최신 상태·대기 중인 작업은
파일 맨 아래에 있다.** 앞부분 몇백 줄만 읽고 "파악 완료"라고 판단하면
안 됨 — 반드시 파일 끝(마지막 `##` 섹션과 그 아래 `[대기]` 항목들)까지
다 읽을 것. 시간이 부족하면 `tail -300` 등으로라도 최신 섹션을
먼저 확인한 뒤 필요한 만큼 위로 거슬러 올라갈 것.

- 버그 수정의 상세 기술 내역(코드 diff, 검증 로그)은
  `PROGRESS-AI제거.md`에 있음 — `작업메모장.md`는 그걸 반복하지
  않고 요약·연결만 한다.
- `template.html`/`dist/taleforge.html` 안에도 레거시 변경 로그
  블록이 파일 최상단에 남아있지만 [11] 항목에서 갱신이 중단됐다.
  그 이후 모든 기록은 `작업메모장.md`에 있다 — 새 항목도 거기에
  추가할 것, 두 곳으로 쪼개지 말 것.
- **[2026-09-18~] 기록 순서 — "끝나고 기록"이 아니라 "논의 시작
  시점에 먼저 기록, 진행되며 계속 갱신"이다.** 사용자와 방향을
  논의하거나 계획을 정하는 단계(코드를 아직 하나도 안 고친 순수
  논의 라운드 포함)부터 곧바로 `작업메모장.md`에 새 섹션을 만들어
  적어두고, 결정이 바뀌거나 작업이 진행될 때마다 그 섹션을 계속
  고쳐 나간다 — 작업이 다 끝난 뒤에 한꺼번에 요약해서 적는 방식이
  아니다. (예: 13번 섹션은 코드 변경 없이 AI 활용 전략만 논의한
  라운드인데도, 논의가 끝나자마자 바로 기록했다 — 이게 표준.)
- **[2026-09-19~] 기록 내용 — "뭘 했다"뿐 아니라 "무슨 과정 중에
  왜/어떻게 발견해서 진행했는지"까지 남긴다.** 결과만 요약하지 말고
  ①원래 뭘 하려고 시작했는지 ②그 과정에서 실제로 뭘 조사/실행하다가
  ③무엇을 발견했고(계획에 없던 것 포함) ④그래서 계획을 어떻게
  바꿨거나 넓혔는지를 순서대로 남긴다(14번 섹션의 "한글 활용형 함정"
  발견 경위처럼 — 결론만 적었으면 나중에 같은 함정에 또 걸린다).
  **완료 여부도 애매하게 두지 않는다** — 항목마다 "완료"/"진행 중"/
  "아직 착수 전"을 명시하고, 완전히 끝난 게 아니면 절대 "완료"라고
  적지 않는다(부분적으로만 됐으면 정확히 어디까지 됐고 뭐가 남았는지
  구체적으로 적는다).
- 매 작업 라운드(논의든 코드 수정이든)가 끝날 때마다 `작업메모장.md`에
  새 항목을 반드시 추가한다. 완료된 항목은 대기열에서 지우고 로그로
  옮긴다.

## 빌드/검증

```
node build.js                                              # dist/taleforge.html 생성 (최초 1회 npm install 필요)
NODE_PATH=/opt/node22/lib/node_modules node tools/pw_final_regression.js   # 회귀 테스트
```
